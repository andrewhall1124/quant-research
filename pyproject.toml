[project]
name = "quant-research"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "polars>=1.43.2",
    "python-dotenv>=1.2.3",
    "thetadata>=1.0.10",
]
